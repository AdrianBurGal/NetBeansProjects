package tema6_cadena_caracteres;

public class BibliotecaString {

    public static void main(String[] args) {

        /*
            \b  --> borrado izquierda
            \n  --> nueva línea
            \r  --> retorno carro
            \t  --> tabulador
            \f  --> nueva página
            \'  --> comilla simple
            \"  --> comilla doble
            \\  --> barra invertida
        
            System.out.println('a' + 1); //se muestra 'b'
            
        - 6.2.1 CLASIFICACIÓN DE CARACTERES -
        
            boolean isDigit(char c)             // devuelve true/false si es o no un caracter.
            boolean isLetter(char c)            // devuelve true/false si es o no una letra.
            boolean isLetterOrDigit(char c)     // devuelve true/false si es o no un alfanumérico.
            boolean isLowerCase(char c)         // devuelve true/false si es o no minúscula.
            boolean isUpperCase(char c)         // devuelve true/false si es o no mayúscula.
            boolean isWhitespace(char c)        // devuelve true/false si es o no un espacio (' ', '\n', '\r', '\t').
        
        - 6.2.2 CONVERSIÓN -
        
            char toLowerCase(char c)            // devuelve un caracter de mayúscula a minúscula.
            char toUpperCase(char c)            // devuelve un caracter de minúscula a mayúscula.
        
        - 6.3.2 COMPARACIÓN -
        
            iguales = cad1.equals(cad2)             // devuelve true/false si ambas cadenas son iguales.
            iguales = cad1.equalsIgnoreCase(cad2)   // devuelve true/false si ambas cadenas son iguales (sin distinguir mayúsculas de minúsculas).
            iguales = cad1.regionMatches(3, cad2, 10, 5) // devuelve true/false al comparar dos fagmentos 
        
        */
        
        
        
        System.out.println("");
    }

}
